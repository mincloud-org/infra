apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pg-admin-staging
  namespace: argocd
  labels:
    app.kubernetes.io/name: pg-admin
    app.kubernetes.io/component: web
    environment: staging
spec:
  project: default
  source:
    repoURL: https://github.com/mincloud-org/infra.git
    targetRevision: develop
    path: workloads/pg-admin/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
